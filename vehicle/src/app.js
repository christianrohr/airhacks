import './MyCow.js';

/*
customElements.whenDefined('my-cow').then(_ => {
    const element = document.querySelector('my-cow');
    window.setTimeout(_ => document.body.removeChild(element), 1000);
    window.setTimeout(_ => document.body.appendChild(element), 2000);
    
}
);
*/

